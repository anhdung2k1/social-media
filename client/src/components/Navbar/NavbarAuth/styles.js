import {makeStyles} from '@mui/styles';

export default makeStyles((theme) => ({
  appBar: {
        borderRadius: 30,
        marginTop: '30px',
        display: 'flex',
        height: '100px',
        flexDirection: 'row',
        alignItems: 'start',
        padding: '10px 50px',
  },
  imageText: {
    marginLeft: '10px',
    marginTop: '5px',
  },
}));